{
	"name": "kit-scene",
	"private": true,
	"description": "Code commun des accessoires de scène (PWA hors-ligne) : écran allumé, portrait, hors-ligne, build et tests",
	"type": "module",
	"engines": {
		"node": ">=24 <25"
	},
	"scripts": {
		"test": "node --test \"tests/**/*.test.ts\"",
		"changelog": "node node/changelog.ts",
		"typecheck": "tsc && tsc -p web && tsc -p sw",
		"check:changelog": "node node/check-changelog.ts"
	},
	"devDependencies": {
		"@types/node": "^24.13.4",
		"typescript": "^7.0.2"
	}
}
